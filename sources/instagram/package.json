{
  "name": "@archiveflow/source-instagram",
  "private": true,
  "version": "0.1.0",
  "description": "Instagram source adapter scaffold (Source #1). No account/media access logic yet — that is a later package (01-instagram-adapter).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@archiveflow/media-model": "workspace:*",
    "@archiveflow/source-sdk": "workspace:*"
  },
  "devDependencies": {
    "@archiveflow/core": "workspace:*",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
