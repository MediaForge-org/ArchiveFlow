{
  "name": "@archiveflow/protocol",
  "private": true,
  "version": "0.1.0",
  "description": "Versioned message envelope and stable error DTO shared between desktop, extensions and Tauri commands.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
