{
  "name": "@archiveflow/source-sdk",
  "private": true,
  "version": "0.1.0",
  "description": "Versioned contract every ArchiveFlow source adapter implements. Must never depend on a concrete source.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@archiveflow/media-model": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
